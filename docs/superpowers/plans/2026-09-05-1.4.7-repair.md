# NekoBox 1.4.7 repair plan

**Goal:** Independently verify the Antigravity handoff and repair reproducible defects without changing the current UI/Router architecture.

**Scope:** Backup safety, Router membership and selection, homepage lifecycle, version metadata and local-only configuration protection. Keep Room schema 10 and arm64-only packaging. Do not commit, push, or upload private backups. Do not inspect protected A7/isA8 configurations.

- [ ] Run the existing offline JVM suite as a baseline.
- [ ] Backup: reproduce silent serialization of missing beans; reject incomplete exports without deleting database rows; read related tables in one transaction; capture document-export state safely across view recreation. Validate imports before destructive replacement.
- [ ] Membership: prevent missing beans from becoming usable Router nodes; reconcile rows and selections atomically; test drag-order updates against concurrent member replacement.
- [ ] UI/runtime: independently review configuration generation, group switching and lifecycle; repair confirmed failures (including recursive empty-group reload), adding focused regression coverage.
- [ ] Set VERSION_NAME=1.4.7 and increment the raw VERSION_CODE from 49 to 50 (effective Android code 250).
- [ ] Protect private configuration/log/key artifacts with ignore rules; check tracked paths and APK entries without printing private content.
- [ ] Run JVM tests, Android-test compilation, lint, debug APK build, ABI and APK metadata checks. Distinguish device tests from host-only checks.
- [ ] Review final diff and record evidence, limitations and artifact paths in a repair report.

Validation uses the task-local JDK 17 and Android SDK; Gradle output stays in ignored local logs. No new dependencies or permanent services are required.
